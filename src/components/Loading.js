import React from 'react'
import LinearProgress from 'material-ui/LinearProgress'

const LoadingProgress = (props) => (
  <div>Loading... <LinearProgress mode='indeterminate' /> </div>
)

export default LoadingProgress
